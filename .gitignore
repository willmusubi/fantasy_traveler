node_modules
dist
dist-ssr
*.local
.DS_Store

# Editor
.vscode/*
!.vscode/extensions.json
.idea

# Logs
logs
*.log
npm-debug.log*

# Env / secrets (API key lives in browser localStorage, never committed)
.env
.env.*
!.env.example

# Test / coverage
coverage
.gstack/

# TypeScript incremental build cache
*.tsbuildinfo

# --- Personal AI tooling, design notes & planning (kept local, not shared) ---
.claude/
.impeccable.md
docs/specs/

# --- Character assets: bring your own (see public/portraits/README.md). The shipped cast
#     (米拉 / 薇拉 / 诺娃) is an ORIGINAL placeholder; portrait / art files are generated
#     locally and never committed, to keep the repo lean and IP-free. ---
docs/asset-pipeline/
docs/avator-pics/
public/portraits/**/*.png
public/portraits-px/
public/art/

# --- Local content override (personal / licensed cast + story). The shipped content is an
#     ORIGINAL IP-free sample; a gitignored pack here swaps it in locally. See src/content/README.md.
#     Never committed — keeps the public repo IP-free. ---
src/content/local/
public/sprites/

# Python caches (from local art tooling)
__pycache__/
*.pyc
